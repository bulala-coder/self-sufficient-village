import React from 'react'
export default function SelfScore({state,tasks,completedCount}){ const taskScore=(completedCount/tasks.length)*45; const xpScore=Math.min(state.xp/1000,1)*25; const journalScore=Math.min(state.journal.length/20,1)*15; const safetyScore=15; const total=Math.round(Math.min(100,taskScore+xpScore+journalScore+safetyScore)); return <div className="grid lg:grid-cols-3 gap-5"><section className="card p-8 text-center"><p className="text-soil/70">你的目前自足度</p><div className="text-7xl font-black text-moss mt-4">{total}%</div><p className="text-soil/75 mt-5 leading-7">這不是評比，而是幫你看見目前已建立哪些生活能力，還有哪些可以慢慢補強。</p></section><section className="lg:col-span-2 card p-6"><h1 className="text-2xl font-black text-bark">分數來源</h1><Score label="任務完成度" value={Math.round(taskScore)} max={45}/><Score label="XP 成長" value={Math.round(xpScore)} max={25}/><Score label="日誌紀錄" value={Math.round(journalScore)} max={15}/><Score label="健康急救基礎" value={safetyScore} max={15}/><div className="bg-cream rounded-3xl p-5 mt-6"><b className="text-bark">村長建議：</b><p className="mt-2 text-soil/80">接下來優先補強「水與能源」、「食物保存」與「地形風險」。自足生活的根基，是知道風險在哪裡，並且提前做小準備。</p></div></section></div> }
function Score({label,value,max}){return <div className="mt-5"><div className="flex justify-between text-sm"><span>{label}</span><b>{value}/{max}</b></div><div className="h-3 bg-cream rounded-full overflow-hidden mt-2"><div className="h-full bg-leaf" style={{width:`${(value/max)*100}%`}}/></div></div>}
